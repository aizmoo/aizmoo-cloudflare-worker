name: Xray Proxy Server

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'How long to keep the proxy running (in minutes)'
        required: false
        default: '30'

jobs:
  run-xray:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Xray
        run: |
          echo "Installing Xray..."
          wget https://github.com/XTLS/Xray-core/releases/latest/download/Xray-linux-64.zip
          unzip Xray-linux-64.zip
          chmod +x xray
          ./xray version

      - name: Install Cloudflared
        run: |
          echo "Installing Cloudflared (Cloudflare Tunnel)..."
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -O cloudflared
          chmod +x cloudflared
          sudo mv cloudflared /usr/local/bin/
          cloudflared --version
          echo "✅ Cloudflared installed (no token required)"

      - name: Create Xray config
        run: |
          cat > config.json << 'EOF'
          {
            "log": {
              "loglevel": "warning"
            },
            "inbounds": [
              {
                "port": 10186,
                "protocol": "vless",
                "settings": {
                  "clients": [
                    {
                      "id": "e4e6cef9-8308-44ff-9783-16441309096d",
                      "flow": "xtls-rprx-vision"
                    }
                  ],
                  "decryption": "none"
                },
                "streamSettings": {
                  "network": "tcp",
                  "security": "reality",
                  "realitySettings": {
                    "dest": "www.microsoft.com:443",
                    "serverNames": ["www.microsoft.com"],
                    "privateKey": "4G2f9-bVbRs4BYQM-lVMqugGgyl7pRlBe5P98TBvfUI",
                    "shortIds": [""]
                  }
                },
                "sniffing": {
                  "enabled": true,
                  "destOverride": ["http", "tls", "quic"]
                }
              }
            ],
            "outbounds": [
              {
                "protocol": "freedom",
                "tag": "direct"
              },
              {
                "protocol": "blackhole",
                "tag": "block"
              }
            ]
          }
          EOF
          echo "Xray configuration created"
          cat config.json

      - name: Get Server IP Information
        run: |
          echo "================================"
          echo "Server IP Information:"
          echo "================================"
          echo "Public IPv4:"
          curl -4 -s https://api.ipify.org || echo "Failed to get IPv4"
          echo ""
          echo "Public IPv6:"
          curl -6 -s https://api.ipify.org || echo "No IPv6 available"
          echo ""
          echo "Detailed IP Info:"
          curl -s https://ipinfo.io || echo "Failed to get detailed info"
          echo ""
          echo "================================"
          echo "Xray Server Details:"
          echo "================================"
          echo "Protocol: VLESS"
          echo "Port: 10186"
          echo "UUID: e4e6cef9-8308-44ff-9783-16441309096d"
          echo "Flow: xtls-rprx-vision"
          echo "Network: tcp"
          echo "Security: reality"
          echo "SNI: www.microsoft.com"
          echo ""
          if [ -n "$TUNNEL_URL" ]; then
            echo "================================"
            echo "Cloudflare Tunnel Public Access:"
            echo "================================"
            echo "Public URL: $TUNNEL_URL"
            echo "Host: $TUNNEL_HOST"
            echo "Port: $TUNNEL_PORT"
            echo "================================"
            echo "Connection Details:"
            echo "================================"
            echo "Server: $TUNNEL_HOST"
            echo "Port: $TUNNEL_PORT"
            echo "UUID: e4e6cef9-8308-44ff-9783-16441309096d"
            echo "Flow: xtls-rprx-vision"
            echo "Network: tcp"
            echo "Security: reality"
            echo "SNI: www.microsoft.com"
            echo "================================"
          fi

      - name: Start Xray
        run: |
          echo "Starting Xray proxy server..."
          ./xray run -config config.json &
          XRAY_PID=$!
          echo "Xray started with PID: $XRAY_PID"
          
          # Wait a moment for Xray to start
          sleep 5
          
          # Check if Xray is running
          if ps -p $XRAY_PID > /dev/null; then
            echo "✅ Xray is running successfully on port 10186"
          else
            echo "❌ Xray failed to start"
            exit 1
          fi
          
          # Check if port is listening
          if netstat -tuln | grep -q ":10186"; then
            echo "✅ Port 10186 is listening"
          else
            echo "⚠️  Port 10186 might not be accessible"
          fi

      - name: Start Cloudflare Tunnel
        run: |
          echo "Starting Cloudflare Tunnel..."
          # Start cloudflared TCP tunnel in background and capture output
          cloudflared tunnel --url tcp://localhost:10186 > tunnel.log 2>&1 &
          TUNNEL_PID=$!
          echo "Cloudflared started with PID: $TUNNEL_PID"
          
          # Wait for tunnel to establish
          echo "Waiting for tunnel to establish..."
          sleep 10
          
          # Extract tunnel URL from logs
          # Cloudflared outputs to stderr, format: tcp://xxxx.trycloudflare.com:xxxxx
          TUNNEL_URL=$(grep -oE 'tcp://[a-z0-9-]+\.trycloudflare\.com:[0-9]+' tunnel.log 2>/dev/null | head -1)
          
          if [ -z "$TUNNEL_URL" ]; then
            echo "⚠️  Trying to extract URL from log again..."
            sleep 3
            # Try different patterns
            TUNNEL_URL=$(tail -30 tunnel.log | grep -oE 'tcp://[^ ]+' | head -1 || echo "")
          fi
          
          if [ -z "$TUNNEL_URL" ]; then
            echo "⚠️  Checking full tunnel log..."
            cat tunnel.log
            # Last attempt - look for any URL pattern
            TUNNEL_URL=$(grep -iE '(tcp://|trycloudflare)' tunnel.log | grep -oE 'tcp://[^ ]+' | head -1 || echo "")
          fi
          
          if [ -n "$TUNNEL_URL" ]; then
            echo "✅ Cloudflare Tunnel established"
            echo "Public URL: $TUNNEL_URL"
            # Extract host and port from tunnel URL (format: tcp://xxxx.trycloudflare.com:12345)
            TUNNEL_HOST=$(echo $TUNNEL_URL | sed 's|tcp://||' | cut -d: -f1)
            TUNNEL_PORT=$(echo $TUNNEL_URL | sed 's|tcp://||' | cut -d: -f2)
            echo "TUNNEL_HOST=$TUNNEL_HOST" >> $GITHUB_ENV
            echo "TUNNEL_PORT=$TUNNEL_PORT" >> $GITHUB_ENV
            echo "TUNNEL_URL=$TUNNEL_URL" >> $GITHUB_ENV
          else
            echo "❌ Failed to get Cloudflare Tunnel URL"
            echo "Tunnel log:"
            cat tunnel.log || true
            exit 1
          fi

      - name: Keep Alive
        run: |
          DURATION=${{ github.event.inputs.duration }}
          if [ -z "$DURATION" ]; then
            DURATION=30
          fi
          
          echo "Keeping proxy alive for $DURATION minutes..."
          echo ""
          echo "================================"
          echo "Connection Information:"
          echo "================================"
          if [ -n "$TUNNEL_URL" ]; then
            echo "🌐 Public Access via Cloudflare Tunnel:"
            echo "  Server: $TUNNEL_HOST"
            echo "  Port: $TUNNEL_PORT"
            echo "  Full URL: $TUNNEL_URL"
          else
            echo "⚠️  Tunnel URL not available"
            PUBLIC_IP=$(curl -4 -s https://api.ipify.org)
            echo "  Server: $PUBLIC_IP"
            echo "  Port: 10186"
          fi
          echo ""
          echo "Xray Configuration:"
          echo "  UUID: e4e6cef9-8308-44ff-9783-16441309096d"
          echo "  Flow: xtls-rprx-vision"
          echo "  Network: tcp"
          echo "  Security: reality"
          echo "  SNI: www.microsoft.com"
          echo ""
          echo "Connection will remain active until: $(date -d "+$DURATION minutes" 2>/dev/null || date -v +${DURATION}M)"
          echo "================================"
          
          # Keep the workflow running
          SECONDS=$((DURATION * 60))
          for ((i=1; i<=SECONDS; i++)); do
            if [ $((i % 60)) -eq 0 ]; then
              REMAINING=$((SECONDS - i))
              echo "⏱️  Still running... $(($REMAINING / 60)) minutes remaining"
            fi
            sleep 1
          done
          
          echo "✅ Proxy session completed"

      - name: Cleanup
        if: always()
        run: |
          echo "Stopping services..."
          echo "Stopping Cloudflare Tunnel..."
          pkill -f cloudflared || true
          echo "Stopping Xray..."
          pkill -f xray || true
          echo "✅ Cleanup completed"

